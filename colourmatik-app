#!/usr/bin/env bash
# colourMatik — start the local web app, then open http://localhost:8765
set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$DIR"
echo "colourMatik açılıyor →  http://localhost:8765   (kapatmak için Ctrl+C)"
exec "$DIR/.venv/bin/python" -m colourmatik.webapp
