# colourMatik — optional LOCAL AI dependencies (Apple Silicon / MPS).
# Enables "Cinematic AI" (CanonCGT) + the SegFormer scene-aware Accurate mode.
# The engine runs fine without these (classical methods); install for the AI modes.
torch>=2.2
torchvision>=0.17
transformers>=4.52   # EoMT (MIT segmentation) needs 4.52+; SegFormer fallback below that
gdown>=5.1
geomloss>=0.2.6
